const MissionUtils = require("@woowacourse/mission-utils");
const OutputView = require("./View/OutputView");

const continueGame = {
  // gameContinueCheck(upBridge, downBridge, bridgeArray) {
  //   MissionUtils.Console.print(`${upBridge} , ${downBridge} , ${bridgeArray}`);
  //   // OutputView.printMap(upBridge, downBridge);
  // },
};

module.exports = continueGame;
