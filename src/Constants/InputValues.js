const INPUT_CHAR = {
  up: 'U',
  down: 'D',
};

const INPUT_NUM = {
  up: '1',
  down: '0',
};

const INPUT_RETRY = {
  restart: 'R',
  quit: 'Q',
};

const INPUT_TYPE = {
  move: 'MOVE',
  retry: 'RETRY',
};

module.exports = {
  INPUT_CHAR,
  INPUT_NUM,
  INPUT_RETRY,
  INPUT_TYPE,
};
