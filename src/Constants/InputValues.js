const INPUT_CHAR = Object.freeze({
  up: 'U',
  down: 'D',
});

const INPUT_NUM = Object.freeze({
  up: '1',
  down: '0',
});

const INPUT_RETRY = Object.freeze({
  restart: 'R',
  quit: 'Q',
});

module.exports = {
  INPUT_CHAR,
  INPUT_NUM,
  INPUT_RETRY,
};
