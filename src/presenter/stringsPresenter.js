const PLAYER_STATE = {
  NEXT: "NEXT",
  FINISH: "FINISH",
  RETRY: "RETRY",
  SUCCESS: "SUCCESS",
  FAIL: "FAIL",
};

const CHECK_PLAYER_STATE = (isFinish, isMove) => {};

module.exports = { PLAYER_STATE };
