const MOVE_RESULT = {
    CORRECT: 'O',
    WRONG: 'X'
};

const GAME_RESULT = {
    PASS: 'P',
    FAIL: 'F'
};

const BRIDGE_SIZE = {
    START: 3,
    END: 20
};

const BRIDGE_MOVING = {
    UP: 'U',
    DOWN: 'D'
};

const GAME_COMMAND = {
    RESTART: 'R',
    QUIT: 'Q'
};
  
module.exports = { MOVE_RESULT, GAME_RESULT, BRIDGE_SIZE, BRIDGE_MOVING, GAME_COMMAND };