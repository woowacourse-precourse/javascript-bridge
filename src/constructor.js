const MOVEMENT = {
  UP: 'U',
  DOWN: 'D',
}

const COMMAND = {
  RETRY: 'R',
  QUIT: 'Q',
}

const BRIDGE_SIZE = {
  MIN: 3,
  MAX: 20,
}

const BRIDGE_POSITION = {
  UP: 1,
  DOWN: 0,
}

module.exports = {
  MOVEMENT,
  COMMAND,
  BRIDGE_SIZE,
  BRIDGE_POSITION,
}