/**
 * String 값을 Nubmer로 변환하는 함수
 * @param {string} string - The string to be converted
 * @returns {number} - The converted number
 */
function toNumber(string) {
  return Number(string);
}

module.exports = {
  toNumber,
};
