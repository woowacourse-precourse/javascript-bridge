const BRIDGE_SIZE = {
  MINIMUM: 3,
  MAXIMUM: 20,
};

const MOVE = {
  UP: 'U',
  DOWN: 'D',
};

const MOVE_PICK = {
  RIGHT: 'O',
  WRONG: 'X',
};

const PLAY = {
  RESTART: 'R',
  QUIT: 'Q',
};

const RESULT = {
  WIN: '성공',
  FAIL: '실패',
};

module.exports = {
  BRIDGE_SIZE,
  MOVE,
  MOVE_PICK,
  PLAY,
  RESULT,
};
