const PATH = {
  RIGHT: 'O',
  WRONG: 'X',
  NOT_CHOSEN: ' ',
};

const BLOCK = {
  UPPER: 'U',
  LOWER: 'D',
};

const COMMAND = {
  RETRY: 'R',
  QUIT: 'Q',
};

module.exports = { PATH, BLOCK, COMMAND };

