const SETTING = {
  MIN_BRIDGE_SIZE: 3,
  MAX_BRIDGE_SIZE: 20,
  MOVING_UP: 'U',
  MOVING_DOWN: 'D',
  GAME_RESTART: 'R',
  GAME_QUIT: 'Q',
  SUCCESS_MOVE: 'O',
  FAIL_MOVE: 'X',
  NOT_MOVE: ' ',
};

module.exports = Object.freeze(SETTING);
