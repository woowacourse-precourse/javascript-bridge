const GAME_RANGE = Object.freeze({
  START_INCLUSIVE: 3,
  END_INCLUSIVE: 20
});

const GAME_COMMAND = Object.freeze({
  UP: 'U',
  DOWN: 'D',
  RETRY: 'R',
  QUIT: 'Q'
});

module.exports = {
  GAME_RANGE,
  GAME_COMMAND
};
