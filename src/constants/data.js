const MIN = 3;
const MAX = 20;
const LOWER_ZONE_NUMBER = 0;
const UPPER_ZONE_NUMBER = 1;
const LOWER_ZONE = "D";
const UPPER_ZONE = "U";

const LENGTH = {
  MIN,
  MAX,
};

const BRIDGE = {
  LOWER_ZONE_NUMBER,
  UPPER_ZONE_NUMBER,
  LOWER_ZONE,
  UPPER_ZONE,
};

module.exports = { LENGTH, BRIDGE };
