const DIRECTION = require('./direction');
const COMMAND_OPTION = require('./command');
const ERROR_MESSAGE = require('./errorMessage');
const BRIDGE_SIZE = require('./bridge');

module.exports = {
  DIRECTION,
  COMMAND_OPTION,
  ERROR_MESSAGE,
  BRIDGE_SIZE,
};
