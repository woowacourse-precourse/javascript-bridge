const MOVE_STRING = {
  UP: 'U',
  DOWN: 'D',
}

const INPUT_RETRY = {
  RETRY: 'R',
  QUIT: 'Q',
}

const INITIAL_VALUE = {
  STEP: -1,
  TRY_NUM: 1,
}

const RESULT_STRING = {
  SUCCESS: '성공',
  FAIL: '실패',
}

const MOVE_RESULT = {
  SUCCESS: 'O',
  FAIL: 'X',
}

module.exports = {MOVE_STRING, INPUT_RETRY, INITIAL_VALUE, RESULT_STRING, MOVE_RESULT};
