const MINIMUM_BRIDGE_LENGTH = 3;
const MAXIMUM_BRIDGE_LENGTH = 20;
const FIRST_PHASE = 1;
const FIRST_TRY = 1;
const UP_STR = "U";
const DOWN_STR = "D";
const CORRECT = "O";
const WRONG = "X";
const QUIT = "Q";
const RETRY = "R";

const BRIDGE_INFO = {
  MINIMUM_BRIDGE_LENGTH,
  MAXIMUM_BRIDGE_LENGTH,
  UP_STR,
  DOWN_STR,
  CORRECT,
  WRONG,
  FIRST_PHASE,
  FIRST_TRY,
  QUIT,
  RETRY,
};

module.exports = { BRIDGE_INFO };
