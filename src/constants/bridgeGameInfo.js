const BRIDGE_GAME = Object.freeze({
  minSize: 3,
  maxSize: 20,
  leftSquareBracket: '[',
  rightSquareBracket: ']',
  verticalLine: '|',
  up: 'U',
  down: 'D',
});

module.exports = {
  BRIDGE_GAME,
};
